# Technology Stack

**Analysis Date:** 2026-03-07

## Languages

**Primary:**
- Java 21 - All application code and tests

## Runtime

**Environment:**
- GraalVM Community Edition 21 - Used for JVM execution and native image compilation
- Gradle 8.14.4 - Build system

**Package Manager:**
- Gradle - Version 8.14.4
- Lockfile: Not detected (Gradle uses lock state managed by dependabot)

## Frameworks

**Core:**
- Picocli 4.7.7 - CLI framework for command-line argument parsing and help generation
- Jackson 2.x (transitive) - JSON serialization/deserialization via ObjectMapper

**Testing:**
- JUnit 5 (Jupiter) 6.0.3 - Test framework with parameterized test support
- JUnit Platform 6.0.3 - Test execution platform
- Hamcrest 3.0 - Matcher library for assertions

**Build/Dev:**
- GraalVM Build Tools 0.11.5 - Native image compilation plugin
- Gradle Shadow 9.3.2 - Fat JAR creation (single-file redistributable)
- Semver Plugin 1.0.4 - Semantic versioning management

## Key Dependencies

**Critical:**
- `io.confluent:kafka-schema-registry-parent:7.8.0` - Confluent Schema Registry parent BOM
- `io.confluent:kafka-schema-registry:7.8.0` - Core schema registry functionality
- `io.confluent:kafka-schema-registry-client:7.8.0` - Client library for schema registry
- `io.confluent:kafka-serde-tools-package:7.8.0` - Serialization/deserialization tools
- `io.confluent:kafka-schema-serializer:7.8.0` - Avro schema serialization support
- `io.confluent:kafka-json-schema-serializer:7.8.0` - JSON Schema serialization support
- `io.confluent:kafka-json-schema-provider:7.8.0` - JSON Schema provider implementation

**Avro Support:**
- `org.apache.avro:avro` (transitive) - Apache Avro schema support and compatibility checking

**Protobuf Support:**
- Protobuf dependencies (transitive) - Protocol Buffers schema support

**Logging:**
- Apache Log4j (commented out in build.gradle but configured in GraalVM native image build)

**Utilities:**
- `org.joda-time:joda-time` (transitive) - Date/time utilities (with special handling for UTC timezone)

## Configuration

**Environment:**
- No environment variables required for basic operation
- Dockerfile-based deployment for containerized execution

**Build:**
- `app/build.gradle` - Main Gradle build configuration
- `settings.gradle` - Gradle settings (root project name and included modules)
- `Dockerfile` - Multi-stage Docker build using GraalVM native-image
- `.github/dependabot.yml` - Automated dependency update scheduling
- `app/src/main/resources/reflection.json` - GraalVM native image reflection configuration

**Version Management:**
- `app/src/main/resources/version.properties` - Version metadata (generated by semver plugin)
- Current version: 0.6.0

## Platform Requirements

**Development:**
- Java 21 (GraalVM Community Edition)
- Gradle 8.14.4
- For native image compilation: GraalVM native-image tool

**Production:**
- Docker container (multi-arch: linux/amd64, linux/arm64)
- Native executable binary (standalone, no Java runtime required)
- Available platforms: macOS-aarch64, Linux-x86_64, Linux-aarch64

**CI/CD:**
- GitHub Actions
- Actions used: actions/checkout (v6), graalvm/setup-graalvm (v1), bats (Bash Automated Testing System)
- Multi-platform builds for Linux (x86_64, ARM64) and macOS (Apple Silicon)

---

*Stack analysis: 2026-03-07*
