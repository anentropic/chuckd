# Roadmap

Chuckd is very much like <https://buf.build/docs/breaking/>

Buf only handles Protobuf though. [The cli](https://github.com/bufbuild/buf) is in Go, OSS. Buf Schema Registry is a SaaS.

- a readymade GitHub action to validate your PRs
- ~add JTD support? msgpack? others?~ why?
- rewrite in Rust (for less tedious build and distribution story)
